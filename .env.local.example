# IMPORTANT: Copy this to .env.local and fill in your actual values
# DO NOT commit .env.local to git!

# Database URL for Prisma
# Replace with your PostgreSQL connection string
DATABASE_URL="postgresql://username:password@localhost:5432/database_name?schema=public"

# Encryption key for storing sensitive data in database
# Generate a secure random key: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your_64_character_hex_encryption_key_here

# NOTE: API credentials (Shopify & Nhanh) are now stored encrypted in the database
# Configure them via the Settings page at /settings after starting the app

# Email Configuration (for password reset emails)
# SMTP settings - use your email provider's SMTP server
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="Your App Name <noreply@yourapp.com>"

# App URL (used in email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Gmail Setup Instructions:
# 1. Enable 2-factor authentication on your Google account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated password as SMTP_PASS
# 4. Use your Gmail address as SMTP_USER

# Other SMTP Providers:
# - SendGrid: smtp.sendgrid.net (port 587)
# - Mailgun: smtp.mailgun.org (port 587)
# - AWS SES: email-smtp.region.amazonaws.com (port 587)
# - Outlook: smtp-mail.outlook.com (port 587)

# Nhanh.vn API Configuration (v3.0)
# API Documentation: https://apidocs.nhanh.vn/v3
NHANH_API_URL=https://pos.open.nhanh.vn
NHANH_APP_ID=your_nhanh_app_id
NHANH_BUSINESS_ID=your_nhanh_business_id
NHANH_ACCESS_TOKEN=your_nhanh_access_token

# Shopify API Configuration
SHOPIFY_STORE_URL=your-store.myshopify.com
SHOPIFY_ACCESS_TOKEN=your_shopify_access_token
SHOPIFY_API_VERSION=2024-01

# App Settings
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_random_secret_here
